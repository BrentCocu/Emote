<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorPrimary">@color/darkgrey</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="actionBarItemBackground">@color/white</item>
        <item name="android:statusBarColor">@color/lightGrey</item>
    </style>

    <style name="Content" parent="AppTheme">
        <item name="background">@color/lightGrey</item>
    </style>

    <style name="GreyEditText" parent="AppTheme">
        <item name="colorControlNormal">@color/darkgrey</item>
        <item name="colorControlActivated">@color/darkgrey</item>
    </style>

    <style name="WhiteEditText" parent="AppTheme">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
    </style>

</resources>
